# Production overrides
services:
  web:
    labels:
      caddy: speechtrainer.mariozechner.at
      caddy.reverse_proxy: "{{upstreams 80}}"
    networks:
      - default
      - caddy-network

  backend:
    networks:
      - default

networks:
  caddy-network:
    external: true